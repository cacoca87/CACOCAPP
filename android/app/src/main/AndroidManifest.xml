<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para streaming y evitar que Android duerma la app -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Leer la música que ya está en el celular.
         Android cambió cómo se pide: desde la versión 13 hay un permiso
         solo para audio (READ_MEDIA_AUDIO), y antes se usaba el de todo
         el almacenamiento. Se declaran los dos, y el viejo con
         `maxSdkVersion` para que en los celulares nuevos NO se pida un
         permiso de "acceder a todos tus archivos" que ya no hace falta
         y que asusta con razón. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:label="CACOCAPP"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- APAGA EL MOTOR DE DIBUJO NUEVO DE FLUTTER (Impeller).
                 Y NO SE SABE POR QUÉ.

                 Esta línea viene del primer commit del proyecto, de
                 antes de que hubiera control de versiones, así que no
                 quedó anotado el motivo. Vale la pena dejar escrito lo
                 que sí se sabe, para que el próximo que la vea no la
                 toque a ciegas ni la deje ahí sin entenderla:

                 * Impeller es el motor nuevo y desde hace varias
                   versiones es el que viene por defecto en Android.
                   Suele ir más suave en las animaciones.
                 * Apagarlo hace que se use el motor viejo (Skia).
                 * El motivo más probable es el reproductor de YouTube:
                   es una vista nativa metida dentro de Flutter, y esa
                   combinación dio problemas con Impeller en varias
                   versiones. O sea que esto puede ser un arreglo de
                   verdad, no una copia sin pensar.

                 NO SE CAMBIÓ porque la diferencia solo se ve en un
                 celular de verdad, y acá no hay ninguno para probarlo.
                 Cambiarlo a ciegas se arriesga justo a romper el video,
                 que es la parte más frágil de la app. Si algún día se
                 prueba: poner el valor en "true", compilar, y mirar
                 sobre todo que el video de Búsqueda Online se siga
                 viendo al achicarlo y al agrandarlo. -->
            <meta-data
                android:name="io.flutter.embedding.android.EnableImpeller"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Declaración del servicio para reproducción en segundo plano -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- Necesario desde Android 11: sin declarar que la app va a
             abrir enlaces, `url_launcher` no "ve" ningún navegador
             instalado y el botón de abrir una noticia falla en silencio.
             Android oculta por defecto qué otras apps hay instaladas. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>